<h1><%= @path %></h1>
<ul>
  <% if archives.length > 0 %>
    <% for entry in archives %>
      <li>
        <a href="<%= entry.url %>"><%= entry.title %></a>
      </li>
    <% end %>
  <% end %>
</ul>

